Day 16[March 19, 2026] - SQL
https://datalemur.com/questions/sql-third-transaction

WITH CTE1 AS (SELECT *, 
ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY transaction_date ASC) AS rnk
FROM transactions)
SELECT user_id, spend, transaction_date
FROM CTE1
WHERE rnk = 3;
